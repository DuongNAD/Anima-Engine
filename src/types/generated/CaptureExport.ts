// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { CaptureAccounting } from "./CaptureAccounting";
import type { CaptureConfig } from "./CaptureConfig";
import type { CaptureStatus } from "./CaptureStatus";
import type { HardwareContext } from "./HardwareContext";
import type { PhaseSummary } from "./PhaseSummary";
import type { UnavailablePhase } from "./UnavailablePhase";
import type { WorkloadContext } from "./WorkloadContext";

/**
 * A capture, as it is written to disk and as the IPC surface returns it.
 */
export type CaptureExport = { schema_version: number, engine_version: string, profile: string, hardware: HardwareContext, workload: WorkloadContext, 
/**
 * Which Bevy executor the schedule ran under while recording.
 */
executor: string, status: CaptureStatus, config: CaptureConfig, accounting: CaptureAccounting, phases: Array<PhaseSummary>, 
/**
 * Phases this build cannot measure, each with the reason.
 */
unavailable: Array<UnavailablePhase>, 
/**
 * Tick number of the oldest and newest retained sample, so a reader can see the window the
 * summary covers rather than assuming it is the whole run.
 */
first_tick: number | null, last_tick: number | null, };
